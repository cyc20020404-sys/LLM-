# =============================================================================
# LLM- 陪伴机器人 / emention_bot - 项目依赖
# GitHub: https://github.com/cyc20020404-sys/LLM-
# 安装: pip install -r requirements.txt
# =============================================================================

# --- 核心：深度学习与训练 ---
torch>=2.0.0
transformers>=4.40.0
datasets>=2.14.0
accelerate>=0.20.0
bitsandbytes>=0.41.0

# --- 训练：SFT / DPO（可选，仅训练时需） ---
trl>=0.7.0
unsloth

# --- Streamlit 对话界面 ---
streamlit>=1.28.0

# --- RAG / 检索：FAISS + BM25 ---
langchain-core>=0.3.0
langchain-community>=0.3.0
langchain-text-splitters>=0.3.0
faiss-cpu>=1.7.4
rank-bm25>=0.2.2

# --- 知识库文档加载（PDF/Word/Txt/MD） ---
pymupdf>=1.23.0
docx2txt>=0.8

# --- 意图识别 ---
scikit-learn>=1.3.0

# --- 通用工具 ---
numpy>=1.24.0
pandas>=2.0.0
tqdm>=4.64.0
pydantic>=2.0.0
python-dotenv>=1.0.0
requests>=2.28.0
huggingface-hub>=0.20.0

# =============================================================================
# 可选依赖（按需安装）
# =============================================================================
# modelscope          # HuggingFace 镜像不可用时，Unsloth 走 ModelScope 下载
# unstructured        # 兜底加载 .doc 等非主流格式
# faiss-gpu           # 有 GPU 时替代 faiss-cpu，加速向量检索

# GGUF 本地推理（CPU 版，GPU 版见 README）：
# llama-cpp-python

# 图 RAG 子模块（graph_rag_deepseek）单独依赖，见 graph_rag_deepseek/requirements.txt：
# neo4j>=5.0.0
# pymilvus>=2.4.0
# sentence-transformers>=3.0.0
# langchain-huggingface>=0.3.0
# openai>=1.0.0
